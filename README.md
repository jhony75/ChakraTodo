# ReactBase

A simple todo application with Chakra-ui

## Commands

`yarn` Installs all the needed packages.

`yarn dev` runs the project in Development mode.

`yarn build` builds the project for deployment.
